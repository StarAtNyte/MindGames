# Core dependencies for SecretMafia Agent
# Python 3.12+ required

# Main game environment
textarena==1.0.0

# HTTP requests for Modal endpoint
requests==2.31.0

# Data processing
numpy==1.26.4

# Modal deployment (optional - only needed if deploying the model)
# modal==0.64.0

# Model dependencies (optional - only needed if deploying the model)
# transformers==4.51.0
# torch==2.0.0
# accelerate==0.21.0
# bitsandbytes==0.41.0
# fastapi==0.104.0
# psutil==5.9.0
# hf_transfer==0.1.5
# tiktoken==0.5.2
